# 引き継ぎメモ（別PC・別Claude会話で再開する人向け）

このファイルは、Claude Codeの会話セッションはPCごとに閉じている（ローカル）ため、
コードと一緒に経緯を残しておくためのメモです。新しい会話を始めたら、まずこのファイルと
`README.md` を読めば文脈が分かります。

## 現在の状態（最終更新: v1.5.2 時点）

- GitHubリポジトリ: https://github.com/uchidaryo56-star/sf6-combo-maker
- まだ **GitHub Pagesでの公開（公開URL発行）はしていない**。次にやるなら
  Settings → Pages → Source: Deploy from a branch / main / (root) でOK。
- アプリの仕組みは [README.md](README.md) を参照（配布・更新・動画共有の手順など）。

## これまでの経緯（ざっくり）

1. SF6（ストリートファイター6）の練習支援アプリを作成。コンボ／フレームデータ／
   セットプレイ／マッチアップメモ／用語集／初心者ガイドの6タブ構成。
2. フレームデータはJP（公式サイト）から実データを取得して収録（`parse_frames.js`で抽出）。
3. 各カードに動画を埋め込めるようにした（YouTube URL or ローカルファイルをIndexedDBに保存）。
4. 用語集はアプリ上で追加・編集・削除できる（既定データへの上書きはブラウザ保存）。
5. 初心者ガイドタブを追加。8段階のロードマップ＋自作SVG図解＋動画埋め込み。
6. 全体的に見やすさ調整（フレーム表の固定ヘッダー・フルワイド表示、本文の文字サイズ拡大、
   カード内ボタンの縮小など）を繰り返し実施。
7. gitリポジトリ化し、GitHubにpush済み（このメモを書いている時点）。

## 次にやろうとしていたこと（中断中）

- **動画の追加作業**: ユーザーが手元に持っている動画ファイルを、どのコンボ／セットプレイ／
  用語に紐づけるか聞いている途中で、引き継ぎ作業（GitHub設定）を先に進めることになった。
  再開したら「動画ファイルの場所」と「使う場所（どのコンボ/用語か）」を聞いて、
  `videos/` フォルダに配置 → `data.js`等に相対パスを設定 → commit/push、の流れで進める。
- **GitHub Pages公開**: リポジトリは出来たが、まだPages設定はしていない。公開URLを
  発行すると、それ以降は更新→push するだけで全ユーザーに反映される（仕組みはREADME参照）。

## 重要な技術的メモ（つまずきやすい点）

- バージョン番号（ヘッダーの `v1.x.x` と `index.html` 末尾の `?v=1.x.x`）は、
  **見た目や挙動を変えるたびに必ず両方上げる**運用にしている（ブラウザキャッシュ対策）。
- ユーザーデータ（追加コンボ・動画・メモ・用語集の編集等）は `localStorage` / `IndexedDB`
  に保存され、配信ファイル（`data.js`等）の更新では消えない設計。
- ローカル動画ファイル共有の判断: 「みんなに公式として配る動画」は `videos/` フォルダに
  置いて相対パスで参照する方式（GitHubにcommitすれば全員に配信される）。「個人用の
  ちょっとしたメモ動画」はアプリ内のIndexedDBローカル保存のままでOKという運用方針。

## 作業環境

- OS: Windows / PowerShell・Git Bash 両方使える環境
- 作業フォルダ: `C:\Users\yokoh\Downloads\ゲーム\sf6-combo-maker`
- git remoteは `origin` で上記GitHub URLに設定済み
